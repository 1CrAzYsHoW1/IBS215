hours = float(input("Введите количество отработанных часов: "))
cred = float(input("Введите остаток по кредиту: "))
eat_money = float(input("Введите количество денег на еду: "))

salary = (200 * hours / 2 ** 3) + hours

if salary >= cred + eat_money:
    print("Часов хватает. Можно отдохнуть 10 минут")
else:
    print("Часов не хватает. Придется работать вечно")